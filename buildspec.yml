---
artifacts:
  files:
    - index.html
    - favicon.ico
    - styles/*
    - images/*
    - dist/bundle.js
    - js/*
phases:
  build:
    commands:
      - "npm run webpack"
  pre_build:
    commands:
      - "npm install"
      - "npm test"
version: 0.1
